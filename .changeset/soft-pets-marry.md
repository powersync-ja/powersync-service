---
'@powersync/service-image': patch
---

Node v24.18.1. When using `unstable_sqlite_expression_engine`, this may change how floating-point values are converted to text, due to the bundled SQLite version being upgraded to 3.53.
