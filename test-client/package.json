{
  "name": "test-client",
  "repository": "https://github.com/powersync-ja/powersync-service",
  "private": true,
  "version": "0.1.62",
  "main": "dist/index.js",
  "bin": "dist/bin.js",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "fetch-operations": "tsgo -b && node dist/bin.js fetch-operations",
    "generate-token": "tsgo -b && node dist/bin.js generate-token",
    "concurrent-connections": "tsgo -b && node dist/bin.js concurrent-connections",
    "build": "tsgo -b",
    "clean": "rm -rf ./dist && tsgo -b --clean"
  },
  "dependencies": {
    "@powersync/service-core": "workspace:*",
    "commander": "^12.0.0",
    "jose": "^4.15.1",
    "undici": "^7.15.0",
    "ws": "^8.18.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.16.2",
    "@types/ws": "~8.2.0"
  }
}